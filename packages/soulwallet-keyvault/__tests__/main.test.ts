import {
    Vault, VaultEvents, SignData,
    Ok, Err, Result,
    ABFA
} from '..';
import { describe, expect, test } from '@jest/globals';

describe('KeyVault', () => {
    test('TODO', () => {
    });
});