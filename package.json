{
  "name": "@soulwallet/soulwallet",
  "private": true,
  "version": "0.0.3",
  "scripts": {
    "typedoc": "typedoc",
    "docs": "pnpm -r run docs",
    "tsc": "tsc",
    "javascript-obfuscator": "javascript-obfuscator",
    "ts-node": "ts-node",
    "preinstall": "npx only-allow pnpm",
    "build": "pnpm -r run build",
    "rebuild": "rm -rf ./packages/*/lib.*;pnpm run build",
    "release": "pnpm -r run release",
    "preTest": "cd soul-wallet-contract/ && chmod +x .env && npm install && npm run build && cd ../bundler/ && yarn && yarn preprocess",
    "test": "pnpm -r run test --filter 'test'",
    "generateABI": "pnpm -r run generateABI --filter 'soulwallet-abi'",
    "generateBytes4": "pnpm -r run generateBytes4 --filter 'soulwallet-decoder'",
    "generateTokens": "pnpm -r run generateTokens --filter 'soulwallet-assets'",
    "clean": "lerna run clean && rm -rf ./packages/*/lib/*",
    "bundlerInit": "cd bundler/ && yarn && yarn preprocess",
    "bundler": "cd bundler/ && yarn run bundler",
    "publish": "pnpm -r publish"
  },
  "devDependencies": {
    "@babel/cli": "^7.22.9",
    "@babel/core": "^7.22.9",
    "@babel/plugin-syntax-flow": "^7.22.5",
    "@babel/preset-env": "^7.22.9",
    "@babel/preset-typescript": "^7.22.5",
    "@changesets/cli": "^2.26.2",
    "@types/node": "^20.4.2",
    "javascript-obfuscator": "^4.0.2",
    "ts-node": "^10.9.1",
    "typedoc": "^0.24.8",
    "typedoc-plugin-markdown": "^3.15.3",
    "typedoc-plugin-missing-exports": "^2.0.0",
    "typescript": "^5.1.6"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": ""
}